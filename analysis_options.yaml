# Shared static-analysis contract for every package in the agentic monorepo.
#
# Package-level `analysis_options.yaml` files include this file and may only
# *relax* rules with an explicit justification comment. Tightening happens here
# so that the whole workspace moves together.
include: package:lints/recommended.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

  errors:
    # A framework's public surface is its contract: undocumented public API and
    # accidental exports are build failures, not suggestions.
    public_member_api_docs: error
    unawaited_futures: error
    always_declare_return_types: error
    avoid_dynamic_calls: error
    close_sinks: error
    cancel_subscriptions: error
    # `todo` comments are allowed in code but never in a released package.
    todo: ignore
    # Dead code and unused elements point at genuine mistakes.
    dead_code: error
    unused_import: error
    unused_local_variable: error
    unused_element: error
    unused_field: error

  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/*.mocks.dart"
    - "**/generated/**"
    - "**/.dart_tool/**"

linter:
  rules:
    # --- Correctness -------------------------------------------------------
    always_use_package_imports: true
    avoid_slow_async_io: true
    avoid_type_to_string: true
    cancel_subscriptions: true
    close_sinks: true
    collection_methods_unrelated_type: true
    comment_references: true
    discarded_futures: true
    literal_only_boolean_expressions: true
    no_adjacent_strings_in_list: true
    no_self_assignments: true
    test_types_in_equals: true
    throw_in_finally: true
    unnecessary_statements: true
    unrelated_type_equality_checks: true
    use_build_context_synchronously: true

    # --- API design --------------------------------------------------------
    always_declare_return_types: true
    avoid_positional_boolean_parameters: true
    avoid_returning_this: true
    avoid_setters_without_getters: true
    avoid_void_async: true
    leading_newlines_in_multiline_strings: true
    one_member_abstracts: false # Ports are intentionally single-method.
    prefer_final_fields: true
    prefer_final_in_for_each: true
    prefer_final_locals: true
    prefer_void_to_null: true
    use_enums: true
    use_setters_to_change_properties: true

    # --- Immutability ------------------------------------------------------
    avoid_field_initializers_in_const_classes: true
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true

    # --- Readability -------------------------------------------------------
    always_put_required_named_parameters_first: false # Ordering follows domain meaning.
    cascade_invocations: true
    directives_ordering: true
    join_return_with_assignment: true
    lines_longer_than_80_chars: false # 80 is too tight for descriptive Dart.
    omit_local_variable_types: false
    prefer_expression_function_bodies: true
    prefer_single_quotes: true
    require_trailing_commas: true
    sort_constructors_first: true
    sort_unnamed_constructors_first: true
    unnecessary_lambdas: true
    unnecessary_parenthesis: true
    use_named_constants: true
    use_string_buffers: true

    # --- Documentation -----------------------------------------------------
    public_member_api_docs: true
    slash_for_doc_comments: true

    # --- Packaging ---------------------------------------------------------
    depend_on_referenced_packages: true
    implementation_imports: true
    secure_pubspec_urls: true
